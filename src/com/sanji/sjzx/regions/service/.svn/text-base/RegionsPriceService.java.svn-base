package com.sanji.sjzx.regions.service;

import java.util.List;

import com.sanji.sjzx.model.RegionsPrice;

public interface RegionsPriceService {
	
	/**
	 * 获取所有节点数据
	 * @Title:gainAllRegionsPrice
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @return 设定文件
	 * @return List<RegionsPrice> 返回类型
	 * @throws
	 * @return
	 */
	public List<RegionsPrice> gainAllRegionsPrice();

	/**
	 * 添加地域价格信息
	 * @Title:addRegionsPrice
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param regionsPrice 设定文件
	 * @return void 返回类型
	 * @throws
	 * @param regionsPrice
	 */
	public void addRegionsPrice(RegionsPrice regionsPrice);
	/**
	 * 根据单品id即goodsSkuId修改地域价格信息
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param id 设定文件
	 * @return void 返回类型
	 * @throws
	 * @param id
	 */
	public void updateRegionsPriceBySkuId(RegionsPrice regionsPrice);
	/**
	 * 根据单品id即goodsSkuId获取该单品相应的地域价格信息
	 * @Title:gainRegionsPriceBySkuId
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param skuId
	 * @param @return 设定文件
	 * @return List<RegionsPrice> 返回类型
	 * @throws
	 * @param skuId
	 * @return
	 */
	public List<RegionsPrice> gainRegionsPriceBySkuId(String skuId);
	/**
	 * 根据订单区域价格id修改地域价格信息
	 * @param regionsPrice
	 */
	public void updateRegionsPriceById(RegionsPrice regionsPrice);
}
