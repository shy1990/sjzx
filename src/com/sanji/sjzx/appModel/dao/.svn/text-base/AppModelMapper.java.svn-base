package com.sanji.sjzx.appModel.dao;

import java.util.List;

import com.sanji.sjzx.model.AppModel;

public interface AppModelMapper {
	
	/**
	 * 添加适用机型信息
	 * @Title:addAppModel
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param  设定文件
	 * @return void 返回类型
	 * @throws
	 */
	public void addAppModel(AppModel appModel);
	
	/**
	 * 根据配件id获取适用机型信息
	 * @Title:gainAppModelByAccId
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param accId
	 * @param @return 设定文件
	 * @return List<AppModel> 返回类型
	 * @throws
	 * @param accId
	 * @return
	 */
	public List<AppModel> gainAppModelByAccId(String accId);
	
	/**
	 * 根据配件id删除相应适用机型信息
	 * @Title:dropAppModelByAccId
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param AccId 设定文件
	 * @return void 返回类型
	 * @throws
	 * @param AccId
	 */
	public void dropAppModelByAccId(String AccId);
	
	/**
	 * 根据适用机型id删除相应记录的适用机型信息
	 * @Title:dropAppModel
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param appModelId 设定文件
	 * @return void 返回类型
	 * @throws
	 * @param appModelId
	 */
	public void dropAppModel(String id);
	
	/**
	 * 根据商品id删除适用机型信息
	 * @Title:dropAppModelByGoodsId
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param ids 设定文件
	 * @return void 返回类型
	 * @throws
	 * @param id
	 */
	public void dropAppModelByGoodsId(String goodsId);
	
	/**
	 * 根据商品id获取适用机型信息
	 * @Title:gainAppModelByGoodsId
	 * @Description:TODO(这里用一句话描述这个方法的作用)
	 * @param @param goodsId
	 * @param @return 设定文件
	 * @return List<AppModel> 返回类型
	 * @throws
	 * @param goodsId
	 * @return
	 */
	public List<AppModel> gainAppModelByGoodsId(String goodsId);
}